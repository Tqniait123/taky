import 'package:flutter/material.dart';

class Locales {
  static const Locale english = Locale('en');
  static const Locale arabic = Locale('ar');
  static const Locale french = Locale('fr');
  static const Locale dutch = Locale('de');
}
